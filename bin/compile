#!/usr/bin/env bash
echo "-----&gt; Installing libccv"
cd $1
curl http://void.cc/libccv-0.1.0.tar.gz -s -O
# make a directory to untar (like unzip) the binary
mkdir -p vendor/libccv
tar -C vendor/libccv -xvf libccv-0.1.0.tar.gz
